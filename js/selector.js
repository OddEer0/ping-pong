const settingsIcon = document.querySelector('.options-icon .fa-gear')
const start = document.querySelector('[data-modal="start"]')
const closeIcon = document.querySelector('.close-icon .fa-xmark')

export const selector = {
   settingsIcon: settingsIcon,
   start: start,
   closeIcon: closeIcon,
}